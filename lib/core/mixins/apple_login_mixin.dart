
mixin AppleLoginMixin {
  Future loginWithAppleHelper() async {
    //TODO: apple sign in here
    // // final AuthorizationResult result = await apple.performRequests([
    // //   AppleIdRequest(requestedScopes: [Scope.email, Scope.fullName])
    // // ]);
  }
}